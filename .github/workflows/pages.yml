name: Deploy DinoCupones to GitHub Pages
on:
  push:
    branches: ["main"]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Validate JavaScript
        shell: bash
        run: |
          cp js/app.js /tmp/app.mjs
          cp js/firebase-service.js /tmp/firebase-service.mjs
          cp js/integrations-config.js /tmp/integrations-config.mjs
          cp js/intro-bootstrap.js /tmp/intro-bootstrap.js
          cp worker/src/index.js /tmp/worker.mjs
          node --check /tmp/app.mjs
          node --check /tmp/firebase-service.mjs
          node --check /tmp/integrations-config.mjs
          node --check /tmp/intro-bootstrap.js
          node --check /tmp/worker.mjs
          node --check tests/ui-contract.mjs
          node tests/ui-contract.mjs
          node mobile/scripts/validate.mjs
      - name: Generate official PWA icon variants
        shell: bash
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y imagemagick
          convert assets/icons/dinocupones-192.png -filter Lanczos -resize 512x512 assets/icons/dinocupones-512.png
          convert assets/icons/dinocupones-192.png -filter Lanczos -resize 390x390 -gravity center -background '#8f5ca8' -extent 512x512 assets/icons/dinocupones-maskable-512.png
      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
